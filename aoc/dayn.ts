import { Day } from ".";
import { debug } from "./utils";

export default [
  function part1(contents: string): number {
    return 0;
  },

  function part2(contents: string): number {
    return 0;
  },
] satisfies Day;
